# Expo's Metro bundler does not follow pnpm's symlinked node_modules reliably.
# Hoisting keeps React Native resolution working inside the workspace.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
